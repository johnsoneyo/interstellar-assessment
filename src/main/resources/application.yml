server:
  port: 8081

spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:mem:interstellar
    driver-class-name: org.h2.Driver
    name: sa
    password: password